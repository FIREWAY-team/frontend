import { LiveDemoView } from "@/features/live/components/live-demo-view";

export const dynamic = "force-dynamic";

/**
 * `/live` — 예선 라이브 5단계 시연 화면. 발표자용.
 */
export default function LivePage() {
  return <LiveDemoView />;
}
