# CLAUDE.md — 골목119 FE · 팀 공용 · 린

> 📌 살아있는 문서. **기술규칙=안정 / API=스펙 확정 후 갱신.** 예시·상세는 `docs/CONVENTIONS.md`(작성 예정), 팀이 정한 것·미정은 `DECISIONS.md`(작성 예정). **화면별 요구사항은 [`docs/FRONTEND_SPEC.md`](docs/FRONTEND_SPEC.md)**.
> **한 줄:** 소방차 진입 어려운 좁은 골목을 차종·CCTV 판독·정적 데이터로 걸러 통과 가능한 경로를 5분 안에 낸다.
> **스택:** Next.js 15 (App Router) · TS · Tailwind · shadcn/ui · Kakao Map SDK · React Query · Zustand · Recharts.
> **데이터=Server-First + Server Action + BFF + httpOnly 쿠키.**
> **⚠️ 로그인 뒤 상황실 도구다.** 공개 페이지가 아니라 SEO·OG는 대상이 아니다. **1440 기준 설계 + 다크가 기본**(야간 상황실).
> ⚠️ **바뀐 결정은 원문을 지우지 말고 `⚠️ YYYY-MM-DD 정정`으로 표기한다** — 다음 사람(사람·AI)이 왜 뒤집혔는지 볼 수 있어야 한다.

---

## 핵심 4원칙

1. **서버우선:** 조회=Server Component, `'use client'`=상호작용 잎사귀(버튼·폼·입력·**지도**)만.
2. **변경(CUD)=Server Action + BFF:** 브라우저→Next서버(액션)→BE. 끝나면 `revalidatePath`. **토큰이 브라우저로 안 나간다**.
3. **정직한 목업:** 목=API 스펙 success 예시 그대로. 모든 화면 **loading/error/empty** 필수.
4. **⚡최적화=처음부터:** 이미지 `next/image`·폰트 `next/font`·무거운거 `next/dynamic`·시맨틱 태그. 쓰는 순간 적용.

---

## 사용자 · 권한

> **MVP는 단일 사용자다.** 역할 축·리소스 소유권 축을 세우지 않는다.

- **사용자 유형 = 상황실 담당자 한 종류.** 관리(차량·CCTV 데이터 확인)와 사용(경로 확정)을 같은 계정이 한다.
- 인증은 **httpOnly 쿠키** 하나. `localStorage` 토큰 금지. 회원가입·비번찾기·소셜 로그인 안 만든다 — 시연 계정 1개.
- 라우트 보호는 `middleware.ts` + 서버 재검사.
- **역할 분리 여지는 컴포넌트 레벨로만 남긴다.** v2에서 관리자·담당자를 나누면 `<CanEdit>` 같은 얇은 가드로 잎사귀만 조건부 렌더링 — 라우트 나누지 않는다.
- ⚠️ **화면 숨김은 UX일 뿐 보안이 아니다.** v2에서 역할이 생기면 Server Action·BFF에서 반드시 재검사.

---

## 라우트 그룹

```
app/
├─ (public)/            /login                          ← 로그인 전
├─ (app)/
│  ├─ /dispatch          ← 메인 상황실 대시보드
│  ├─ /map               ← 관할 지도 (오버레이·CCTV 팝업)
│  └─ /vehicles          ← 차량 관리 (목록·상세)
└─ /api/                 ← BFF 프록시 (BE 호출 · 토큰 주입)
```

- `(app)/*`은 로그인 필수. `middleware.ts`에서 쿠키 확인.
- **`/dispatch`가 얼굴 화면이다.** 심사·시연에서 이 하나로 판정 가능해야 한다.
- 지도 셸(Kakao Map)은 `/dispatch` · `/map` · `/vehicles`(썸네일) 세 곳이 재사용한다 — 컴포넌트로 뽑는다(`components/map/map-canvas.tsx`).
- ⚠️ **명세에 없는 화면·기능은 안 만든다.** 화면 목록은 [`docs/FRONTEND_SPEC.md`](docs/FRONTEND_SPEC.md) §4가 정본.
- 회원가입 · 비밀번호 찾기 · 소셜 로그인 · 관리자 별도 콘솔 · 통계·이력 화면 — **전부 MVP 밖.** 요청이 오면 이 문서와 스펙을 근거로 되돌린다.

---

## 폴더·네이밍

- `src/`: `app/` · `components/`(ui·common·map·domain) · `features/<도메인>/` · `hooks/` · `lib/` · `types/` · `constants/` · `styles/`
- 컴포넌트 `PascalCase`·훅 `useXxx`·액션 `xxxAction`·상수 `UPPER_SNAKE`·boolean `is/has/should`·핸들러 `handle~`/prop `on~`
- props 인터페이스 명시 · **`any` 금지**(`unknown`+가드) · 200줄↑ 분리 · 로직=커스텀훅 · **`enum` 금지**(`as const`)

---

## 렌더링·데이터

- 조회=Server Component `async/await`(useEffect 페칭 X). cache: `force-cache`/`no-store`/`revalidate`.
- 폼=`useActionState`+`useFormStatus`(pending). `redirect()`=try/catch 밖.
- `use client` 최소화 · 데이터는 props로 · client가 server import 말고 `children`.
- **알림·실시간 스트림 없다**(v2 확정 · 팀장 v2 초안 (팀 채널에서 공유)). SSE·WebSocket 코드 새로 안 짠다 — 실시간이 필요하다는 요구가 오면 이 결정을 먼저 뒤집는다.
- 변경 결과 피드백=**토스트**(shadcn `sonner`, `<Toaster />`는 루트 레이아웃 1개). ❌폼 검증 오류(→필드 인라인)·파괴적 작업 확인(→Dialog)·페이지 전체 실패(→`error.tsx`). 토스트는 사라지므로 **보조**다.

### 상태관리 경계

| 상태 종류 | 저장소 | 예 |
|---|---|---|
| 서버 데이터 (재조회 가능) | **React Query** | `/scenarios`, `/route`, `/vehicles`, `/no_go`, `/coverage`, `/cctv/:id` |
| UI 로컬 (컴포넌트 스코프) | `useState` | 팝업 열림 · 폼 입력 |
| UI 로컬 (화면 간 공유) | **Zustand** | 선택된 차량 id · 오버레이 토글 |
| 세션 · 계정 | httpOnly 쿠키 + `GET /auth/me` (React Query) | 로그인 사용자 |

- **Zustand는 오직 UI 상태만.** 서버 데이터는 절대 담지 않는다 — 캐시 무효화·재조회를 React Query에 위임한다.
- ⚠️ 두 저장소에 같은 값을 두면 어느 쪽이 정본인지 다음 사람이 헷갈린다.

---

## 지도 · 이미지 · CCTV 스틸 ⚠️

> **Kakao Map SDK는 브라우저 전용.** 이 프로젝트의 성능·리소스 사용은 여기서 대부분 결정된다.

- **Kakao Map은 `'use client'` + `next/dynamic(ssr:false)` 필수.** SSR에서 로드하면 `window` 참조로 서버가 터진다.
- **API 키는 도메인 등록 방식.** 프론트에 노출되는 값이라 노출 자체는 문제 없지만, **도메인·referrer 잠금**을 걸어 두지 않으면 다른 사이트가 우리 할당량을 태운다 — 카카오 개발자 콘솔에서 배포 도메인 등록 필수.
- **지도 SDK 로드는 앱 루트에서 1회.** 컴포넌트마다 `<script>`를 다시 붙이지 않는다 — 지도 인스턴스가 늘어 메모리·이벤트가 이중으로 걸린다.
- **`bbox` 조회는 debounced(250ms).** 지도 이동·줌마다 API를 부르면 서버·할당량이 쓸려 나간다. `React Query`로 `[bbox, layer]` 조합 캐시 5분 유지.
- **CCTV 스틸 이미지는 S3 → CloudFront에 정적 호스팅.** 프론트는 URL만 소비한다.
- **이미지는 반드시 `next/image`.** CCTV 스틸은 원본 크기가 크다 — 자동 리사이즈·webp·lazy load가 필수. `<img>` 직접 사용 금지.
- **팝업 안 이미지는 lazy.** 골목 클릭 시점에만 `GET /cctv/:edge_id` 호출·이미지 로드. 지도 진입 시 전부 미리 가져오면 첫 화면이 무거워진다.

---

## 도메인 상수

- **`as const` + 라벨맵**으로 정의(`enum` 금지). 코드엔 영문 상수, 화면엔 한글 라벨 — **라벨 하드코딩 금지.**
- 값 목록은 **`constants/`에 정의**한다. 문서에 옮겨 적지 않는다(바뀌면 두 벌이 어긋난다).
- ⚠️ 값은 **BE 담당자 문서로 확인**한 뒤 고친다. BE enum과 이름이 어긋나면 매퍼에서 조용히 틀린다.
- 파생값은 **상태 필드에 넣지 말고 계산**한다. 예) 통과확률은 저장값(`passable_prob`) 하나, **"진입 불가" 배지는 계산**(`prob < 임계값`).

---

## Mock → Live 격리막

- 컴포넌트=**UI계약 타입(`types.ts`)만** 의존. `server.ts`/`actions.ts`가 `isMock` 분기(mock | serverApi).
- **매퍼**가 BE shape → UI계약 흡수. 연동 시 **server·actions·매퍼만 수정, 컴포넌트 0줄.**
- ⚠️ BE 스펙은 v2 초안이라 필드가 바뀔 여지가 크다. **초반 2주는 mock 병행 · 마지막 4일에 라이브 연결.**
- 목이면 목이라고 화면·주석에 명시(§핵심 4원칙 3).

---

## 연동 검증

- **① 담당자 도메인 문서를 받아** 정책·Enum·에러코드를 맞춘다. BE 4명 각자 담당하는 도메인(`/route` · `/no_go` · `/cctv` · `/vehicles`) 문서를 별도로 요청한다.
- **② 구현할 때는 BE 레포 실코드로** 경로·메서드·요청/응답 shape을 재확인한다.
  ⚠️ **문서와 코드가 다르면 코드가 맞다.** 문서는 합의된 규약이지 배포된 스펙이 아니다.
  ⚠️ **Swagger·구두 추측 금지.** 둘 다 못 하면 "가정 shape·미검증" 주석을 남긴다.
- 응답 봉투 모양은 **아직 모른다.** 벗기는 일은 **매퍼 한 곳**에서 하고 컴포넌트는 봉투를 모른다 — 컴포넌트가 봉투를 알면 모양이 바뀔 때 전 화면을 고쳐야 한다.

---

## 시연 시나리오 데이터 ⚠️

> **v2 결정** — 실시간 신고 리스트가 아니라 미리 만든 시연 시나리오 3~5개.

- **정본은 BE `/scenarios` 응답.** 프론트에 시나리오 배열을 하드코딩하지 않는다 — 시연장에서 시나리오 하나 늘리려면 BE·FE 두 곳을 고쳐야 한다.
- 목 단계에서도 `server.ts` 안 시드 배열에 둔다 — 컴포넌트는 `useScenarios()` 훅으로만 접근.
- ⚠️ **시나리오 카드에 "지금 접수됨" 같은 라이브 문구를 쓰지 않는다.** 시연이지 실시간이 아니다(§정직성).

---

## 디자인 토큰 (하드코딩 금지 · **CSS 변수로 정의**)

> 🎨 **화면 만들 때는 [`docs/DESIGN.md`](docs/DESIGN.md)(작성 예정)를 먼저 훑는다.** 여기는 값이고, 거기는 **언제 어떤 값을 고르는지와 틀렸을 때 나는 일**이다.
>
> **다크가 기본이다** (야간 상황실). 라이트는 옵션 토글. 토큰 구조는 Day-1.

- **다크 (기본):** 배경·셸 `#1A1715` · 카드 `#242120` · 보조 `#2E2A28` · 보더 `#33302D` · 텍스트 `#FAFAF9` · 보조텍스트 `#A8A29E`
  - ⚠️ **순검정 금지** (최저값 `#1A1715`).
  - **사이드바·상단바·본문은 `--background` 한 색**, 카드는 `--card`.
- **라이트 (옵션):** 배경·셸 `#FFFFFF` · 카드 `#FFFFFF`+보더 `#E7E5E4` · 섹션띠 `#FAFAF9`
  - 라이트에서 `--background`·`--card`는 **둘 다 흰색**. 카드는 색이 아니라 **얕은 그림자**로 띄운다.
  - ⚠️ **바탕을 회색·크림으로 밀지 않는다.**
- 다크는 **전 페이지 적용**이다. 컴포넌트에서 `dark:` 클래스를 직접 쓰지 말고 토큰만 쓴다.
- **시맨틱:** 액센트 `#3B82F6` · 성공 `#22C55E` · 경고 `#F59E0B` · **에러(진입 불가) `#EF4444`**.
- **색으로 알리는 건 에러(빨강) 하나뿐** — 통과확률 히트맵은 그라디언트를 쓰되, 정확한 판단은 카드의 숫자·문구가 담당한다. 색약 사용자를 고려해 히트맵 옆에는 반드시 숫자·아이콘·문구가 붙는다.
- **포커스 링은 먹색**(`--ring`).
- **레이아웃:** 사이드바 220px + PageLayout 3종 — `list`(1440) · `detail`(1440 2컬럼) · `centered`(560)
  - ⚠️ `w-[1440px]` 대신 `mx-auto max-w-[1440px] px-8`, absolute 대신 flex/grid, 표는 `overflow-x-auto`로 감싼다.
  - ⚠️ **새 숫자를 만들지 않는다.** 1080·1120·1144처럼 중간값이 늘면 화면마다 여백이 달라 눈에 띄게 흔들린다.
  - **모바일 대응 안 함**(v3까지). 대상 화면 선별은 실증 뒤.
- 폼 2열(`FormRow`) · 제출 버튼 하단우측 · 로딩=스켈레톤 · 모션 100/150/250ms · 숫자 `tabular-nums`
- **카피:** **~합니다체** · 날짜 `9월 5일(금)` · 시각 `오전 3시 22분`
  - 상황실은 **초 단위 판단**이 걸린 화면이다. 친근한 말투는 오히려 가볍게 읽힌다. `들어올 수 없어요` → `접근할 수 없습니다`.
  - 명령은 **`~해 주세요`** 를 쓴다. `~하십시오`는 딱딱해서 안 쓴다.
- 아이콘: `lucide-react` 표준 / 커스텀SVG=SVGR(`currentColor`). ❌이모지·`<img src=.svg>`

---

## 컴포넌트 위생·a11y

- **만들기 전 재사용 확인:** `ui` > `common` > `map` > 도메인. 있으면 props화, 없을 때만 신규.
- a11y(RTL `getByRole` 직결): input=`label htmlFor`·버튼 이름/`aria-label`·img `alt`·모달 `role="dialog"`+포커스·클릭은 `button`/`a`.
- **지도 위 클릭 가능한 요소는 키보드 대체 경로 필수** — Tab으로 오버레이·팝업에 진입 가능해야. 마우스 위주 사용자지만 심사·데모에서 걸리면 감점.

---

## 목록·페이지네이션

> **골목119는 목록이 작다** (시나리오 5개 · 차량 3~5대). 무한스크롤·가상화 규칙은 지금 필요 없다.

- 목록이 수십 개 넘어가면(예: v2 이력 화면) z-groupware 규칙(스크롤 트리거·id 중복 거름·전체 건수 헤더)을 그대로 가져온다.
- MVP에서는 **단순 배열 렌더** + `key`로 충분.

---

## 성능

- CWV: **INP<200ms · CLS<0.1**, First Load JS<200KB. 무거운거 `next/dynamic`·개별 import·moment 금지(date-fns/dayjs).
- **Kakao Map SDK가 First Load JS를 가장 많이 먹는다** — `/dispatch` · `/map` 진입 시에만 로드. 로그인·회원 화면에 붙지 않게 컴포넌트 분리.
- CCTV 스틸은 lazy(§지도·이미지) + CloudFront 캐시.

---

## 테스트

- **작성 시점:** 로직/매퍼=짜자마자 · 컴포넌트(RTL)=mock 확정 후 핵심위주 · E2E=실연동 후. **마지막 몰빵 금지.**
- Jest+RTL(`next/jest`·jsdom) · `getByRole` 우선 · AAA · `user-event`.
- E2E=Playwright — 시연 시나리오 골든패스(신고 클릭 → 경로 확정) 하나는 반드시 자동화.
- **지도 컴포넌트는 유닛 테스트 예외** — Kakao SDK가 jsdom에서 안 돈다. 지도 위 로직(경로 색상 계산 · 히트맵 값 매핑)은 **DOM 없는 순수 함수로 뽑아 그것만** 테스트.

---

## AI 기능

- 프론트 키 노출 금지 → **서버 프록시.** LLM 응답=**서버 액션이 문자열로 돌려준다**(스트리밍 안 함, v2 스코프).
- 마크다운=`react-markdown`+`remark-gfm`+**`rehype-sanitize`(XSS)**.
- 대상: **경로 근거 설명(LLM 5줄 + 제외 링크 이유) 하나뿐이다.**
  ⚠️ **"실시간 챗봇" · "음성 명령" · "AI 신고 자동 접수" 없다.** 요구가 오면 이 자리를 먼저 뒤집는다.
- ⚠️ **AI 아닌 것을 AI라 부르지 않는다** — 통과확률 계산(회귀모델)·CCTV 판독(YOLO+SAM2)은 결과가 정적으로 이미 들어와 있는 값이다. 프론트는 AI 여부와 무관하게 "판독 결과"로만 표기.
- ⚠️ 데모는 **목**이다 → 화면·주석에 명시. LLM 실모델 붙는 시점까지 응답은 시드 문자열.

---

## Git·PR

- 이슈 번호가 브랜치·커밋·PR을 잇는다. **작업 전 이슈부터 만든다.**
- 브랜치 `feature/{도메인}-{기능}#{이슈번호}` · `fix/{도메인}-{내용}#{이슈번호}` · `docs/{내용}#{이슈번호}` · `test/{도메인}-{내용}#{이슈번호}`. base는 `develop`(`main`은 릴리즈용).
- 커밋 `type: 제목 #{이슈번호}`(한글 50자). type 9종: feat/fix/style/refactor/docs/chore/test/design/merge.
- PR 본문에 `Closes #{이슈번호}`.
- PR: 무엇·왜·확인법 + 리뷰 1명. ⛔ console·주석코드 커밋 · main/develop 직접push · `any` · 토큰 localStorage.

---

## AI(Claude)와 일하는 법 ⭐

1. **간결하게.** 답변·문서·주석 군더더기 없이. (길이는 프롬프트로만 줄어든다)
2. **요청한 범위만.** 안 시킨 리팩터·추가 파일 금지. 애매하면 **한 줄 묻고 진행**.
3. **검증은 알아서 — "다시 확인해" 지시 금지**(중복 작업). 단 **성공 기준**(스키마·필수 필드·연동 검증)은 명세이므로 유지.
4. **서브에이전트 위임은 크고 독립적인 작업만.** 검증용 위임 금지.
5. **코드리뷰에 "고위험만/보수적으로" 금지** — 전부 보고시키고 필터는 별도 단계.
6. **문서 4벌 정합** (z-groupware 승계) — 정책은 `FRONTEND_SPEC.md`, 기술 규칙은 이 문서, 코드 컨벤션은 `CONVENTIONS.md`, 결정 이력은 `DECISIONS.md`. 정본 순위 명시.

---

## ⚠️[팀확정] (임의로 정하지 말 것)

- [ ] **BE API 스펙 v2 확정** — 초안 팀장 v2 초안 (팀 채널에서 공유) §3 기준, 필드 변경 시 이 문서와 매퍼만 함께 갱신
- [ ] **CCTV 판독 이미지 확보** — 유강현 담당, D-10까지 S3 업로드 여부
- [ ] **시연 시나리오 3~5개 확정** — 위치·차량·기대 결과 · BE 시드 준비
- [ ] **로그인 · 세션 방식** — httpOnly 쿠키 vs API 키 하드코딩
- [ ] **배포 계정** — Vercel 팀 계정 발급 vs 홍근 개인 계정 (도메인은 확정됨, 아래 확정 항목 참고)
- [ ] **Kakao Map 개발자 계정** — 팀 공용 발급 · 배포 도메인 등록
- [ ] **LLM 실모델 선정 시점** — 예선 목 → 본선 실모델 전환 스케줄
- [x] (확정) 스택=**Next.js 15 App Router** · 스타일=Tailwind+shadcn/ui · 상태=React Query + Zustand · 지도=Kakao Map · **1440 기준 + 다크 기본** · **실시간 없음(v2)** · **단일 사용자(MVP)**
- [x] (확정, 2026-09-07) **배포 도메인 `fireroad.shop`** · `next.config.ts` `allowedOrigins` · `layout.tsx` `SITE_URL` 반영 · 카카오 JS SDK 도메인 등록 필요
